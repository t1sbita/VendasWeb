@model VendasWeb.Models.Filial

@{
    ViewData["Title"] = "Adicionar";
}

<h1>Adicionar</h1>

<h4>Filial</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FilialNome" class="control-label">Nome da Filial</label>
                <input asp-for="FilialNome" class="form-control" />
                <span asp-validation-for="FilialNome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Adicionar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar para a lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
